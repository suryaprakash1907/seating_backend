# Railway Deployment Config

[build]
builder = "DOCKERFILE"
dockerfile = "Dockerfile"

[deploy]
# Railway exposes PORT via environment variable
startCommand = "python manage.py runserver 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
